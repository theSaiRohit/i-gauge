import { css } from "@emotion/react";

export const outerElemCss = css`
  display: grid;
  place-items: center;
`;

export const innerElemCss = css`
  max-width: var(--max-page-width);
  width: 90%;
`;
